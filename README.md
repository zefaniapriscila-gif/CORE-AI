# CORE-AI

Prototype aplikasi berbasis React + Vite untuk proyek CORE-AI.

## Requirements

- Node.js 18+
- npm

## Menjalankan Project

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — jalankan development server
- `npm run build` — build production
- `npm run preview` — preview hasil build
.